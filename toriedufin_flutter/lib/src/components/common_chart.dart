import 'package:flutter/material.dart';

class CommonChartWidget extends StatelessWidget {
  const CommonChartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
